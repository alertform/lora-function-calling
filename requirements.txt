# Install torch SEPARATELY with the CUDA build first (see scripts/setup_env.ps1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# Then:  pip install -r requirements.txt
transformers>=4.46,<5
peft>=0.13
trl>=0.12
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.44        # 4-bit QLoRA; ships official Windows wheels since 0.43
sentencepiece
pyyaml
tqdm
